.ingredient-item { display: flex; justify-content: space-between; align-items: center; padding: var(--space-xs) 0; border-bottom: 1rpx solid var(--color-divider); }
.ing-name { font-size: var(--font-sm); }
.ing-amount { font-size: var(--font-sm); color: var(--color-text-secondary); }
